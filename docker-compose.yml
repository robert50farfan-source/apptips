services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATA_DIR=/app/data
    volumes:
      - app-data:/app/data
    networks:
      - dokploy-network

volumes:
  app-data:

networks:
  dokploy-network:
    external: true
